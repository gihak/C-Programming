/****************************************************************************

[1] 5자리의 자연수(10000~99999)를 입력받아 자리가 역순인 수로 변환한 후 출력하는 프로그램 작성.
㈜ 5자리 자연수는 자릿수별로 하나씩 입력받고 역순의 자연수는 하나의 자연수로 출력할 것)

예: 26753--> 35762, 12300-->321

*****************************************************************************/

/*
#include <stdio.h> // 표준 입출력 함수(printf, scanf 등)를 사용하기 위해 필요한 헤더 파일.

int main (void) { // 프로그램의 시작점인 main 함수.

    // 5자리 숫자의 각 자리를 저장할 정수형 변수 5개를 선언.
    int n1, n2, n3, n4, n5; 
    
    // 입력받은 자릿수들로 조합할 원본 숫자와 역순 숫자를 저장할 변수.
    int original_num, reversed_num;

    // 첫 번째 자리 숫자 io.
    printf("첫 번째 자리수를 입력하세요: ");
    // 사용자로부터 정수를 입력받아 변수 n1에 저장.
    scanf("%d", &n1);

    // 두 번째 자리 숫자 io.
    printf("두 번째 자리수를 입력하세요: ");
    scanf("%d", &n2);

    // 세 번째 자리 숫자 io
    printf("세 번째 자리수를 입력하세요: ");
    scanf("%d", &n3);

    // 네 번째 자리 숫자 io.
    printf("네 번째 자리수를 입력하세요: ");
    scanf("%d", &n4);

    /// 다섯 번째 자리 숫자 io
    printf("다섯 번째 자리수를 입력하세요: ");
    scanf("%d", &n5);

    // 입력된 각 자릿수에 10의 거듭제곱을 곱하여 원래의 5자리 숫자를 만듦
    // 예: 2, 6, 7, 5, 3 입력 시 -> (2 * 10000) + (6 * 1000) + (7 * 100) + (5 * 10) + 3 = 26753
    original_num = (n1 * 10000) + (n2 * 1000) + (n3 * 100) + (n4 * 10) + n5;
    
    // 자릿수의 순서를 반대로 하여 역순의 숫자를 만듦
    // 예: 2, 6, 7, 5, 3 입력 시 -> (3 * 10000) + (5 * 1000) + (7 * 100) + (6 * 10) + 2 = 35762
    reversed_num = (n5 * 10000) + (n4 * 1000) + (n3 * 100) + (n2 * 10) + n1;

    // 원본 숫자를 화면에 출력
    printf("\n입력된 숫자: %d\n", original_num);
    
    // 역순으로 변환된 숫자를 화면에 출력
    printf("역순으로 변환된 숫자: %d\n", reversed_num);

    return 0;
}
*/



/****************************************************************************

[2] 5자리의 자연수(10000~99999)를 입력받아 자리가 역순인 수로 변환한 후 출력하는 프로그램 작성.
㈜ 5자리 자연수는 자릿수별이 아닌 하나의 자연수로 입출력 할 것)

예: 26753--> 35762, 12340-->4321

*****************************************************************************/

/*
#include <stdio.h> // C언어의 표준 입출력(Standard Input/Output) 함수를 사용하기 위해 포함하는 헤더 파일

int main (void) { // 프로그램의 시작점인 main 함수

    // 5개의 정수형 변수를 선언. 이 변수들은 5자리 숫자의 각 자릿수를 저장하는 데 사용됨.
    int n1, n2, n3, n4, n5; 
    
    // 사용자가 입력한 원본 숫자와 그 숫자를 뒤집은 결과를 저장할 정수형 변수 2개를 선언.
    int original_num, reversed_num;

    // 사용자에게 5자리 자연수를 입력하라는 메시지를 화면에 출력함.
    printf ("5자리의 자연수(10000~99999)를 입력하세요: ");
    
    // 사용자로부터 입력을 받음
    // %1d는 정수(d)를 한(1) 자리씩만 읽으라는 의미.
    // 예를 들어, 사용자가 12345를 입력하면, scanf는:
    // %1d가 1을 읽어서 n1에 저장
    // %1d가 2를 읽어서 n2에 저장
    // %1d가 3을 읽어서 n3에 저장
    // %1d가 4를 읽어서 n4에 저장
    // %1d가 5를 읽어서 n5에 저장
    scanf("%1d%1d%1d%1d%1d", &n1, &n2, &n3, &n4, &n5); //예2: " 93802" %1d는 공백을 무시하고 9만 읽어 변수에 저장하고 나머지는 버퍼에 남음
   
    // 각 자릿수에 10의 거듭제곱을 곱하고 더해서 원래의 5자리 숫자 계산
    // 예: n1 = 1, n2 = 2, n3 = 3, n4 = 4, n5 = 5 라면 (1 * 10000) + (2 * 1000) + (3 * 100) + (4 * 10) + 5 = 12345
    original_num = (n1 * 10000) + (n2 * 1000) + (n3 * 100) + (n4 * 10) + n5;
    
    // 위와 같은 원리로, 자릿수의 순서를 뒤집어서 새로운 숫자를 만듦.
    // 예: n1 = 1, n2 = 2, n3 = 3, n4 = 4, n5 = 5 라면 (5 * 10000) + (4 * 1000) + (3 * 100) + (2 * 10) + 1 = 54321
    reversed_num = (n5 * 10000) + (n4 * 1000) + (n3 * 100) + (n2 * 10) + n1;

    // 재구성한 원본 숫자를 화면에 출력
    printf("\n입력된 오리지널 숫자: %d\n", original_num);
    
    // 역순으로 만든 숫자를 화면에 출력
    printf("역순으로 변환된 숫자: %d\n", reversed_num);

    return 0;
}
*/



/*****************************************************************************************************************

[3] 학생의 영어 및 국어 점수(0~100점의 자연수)와 이름(알파벳 1자)을 아래와 같이 한줄에 하나씩 입력받아 다음과 같은 형태로 출력하는 프로그램 작성. 
(평균은 소숫점 2자리까지)

<입력 예시>: 은 입력 데이타
Eng: 87
Kor: 92
Name: S

<출력>
NAME    ENGLISH       KOREAN      TOTAL      AVERAGE
        SCORE         SCORE       SCORE      SCORE
S       87            92          179        89.50

******************************************************************************************************************/

/*
#include <stdio.h> // 표준 입출력 함수(printf, scanf 등)를 사용하기 위해 필요한 헤더 파일

int main (void) { // 프로그램의 시작점인 main 함수

    // 변수 선언부
    int eng_score, kor_score, total_score; // 정수형 변수: 영어, 국어, 총점
    float avg_score; // 실수형 변수: 평균 점수 (소수점이 나올 수 있으므로 float 사용)
    char stu_name; // 문자형 변수: 학생 이름 (알파벳 1자)

    // 학생의 영어 점수 입력 및 저장
    printf ("Eng: "); // 사용자에게 "Eng: " 
    scanf ("%d", &eng_score); // 정수 값을 입력받아 eng_score 변수에 저장

    // 학생의 국어 점수 입력 및 저장
    printf ("Kor: "); // 사용자에게 "Kor: "
    scanf ("%d", &kor_score); // 정수 값을 입력받아 kor_score 변수에 저장함

    // 학생의 이름(알파벳 1자) 입력 및 저장
    printf ("Name: "); // 사용자에게 "Name: " 메시지를 보여줌.

    // 문자를 입력받아 stu_name 변수에 저장
    scanf (" %c", &stu_name); 

    // 총점과 평균 점수를 계산
    total_score = eng_score + kor_score; // 두 과목 점수를 더해 총점을 구함
    avg_score = total_score / 2.0; // 총점을 2.0으로 나누어 평균을 구함
                                   // 2가 아닌 2.0으로 나누어 실수 연산이 되도록 함

    printf ("\n================ RESULT ================\n");

    // 결과 출력 부분
    // 헤더(제목) 부분을 출력. \t는 탭(일정한 간격)을 의미
    printf ("\nNAME\tENGLISH\tKOREAN\tTOTAL\tAVERAGE");
    printf ("\n\tSCORE\tSCORE\tSCORE\tSCORE");
    
    // 입력받은 값과 계산된 결과를 형식에 맞춰 출력.
    // %c: 문자, %d: 정수, %.2f: 소수점 둘째 자리까지의 실수
    printf ("\n%c\t%d\t%d\t%d\t%.2f\n", stu_name, eng_score, kor_score, total_score, avg_score);

    return 0;
}
*/
